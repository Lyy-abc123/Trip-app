# 部署指南 - 如何分享应用给伴侣

有几种方式可以将应用分享给您的伴侣，推荐使用在线部署方式。

## 方式一：部署到 Vercel（推荐，最简单）⭐

Vercel 提供免费的静态网站托管，非常适合 React 应用。

### 步骤：

1. **准备代码**
   - 确保代码已提交到 Git（GitHub、GitLab 或 Gitee）
   - 如果没有 Git 仓库，先创建一个：
     ```bash
     git init
     git add .
     git commit -m "Initial commit"
     ```

2. **注册 Vercel**
   - 访问 https://vercel.com/
   - 使用 GitHub/GitLab 账号登录（或注册新账号）

3. **部署项目**
   - 点击 "Add New Project"
   - 导入您的 Git 仓库
   - 配置项目：
     - Framework Preset: 选择 "Vite"
     - Root Directory: `trip-app`（如果仓库在子目录）
     - Build Command: `npm run build`
     - Output Directory: `dist`
   - 添加环境变量（如果使用 Firebase）：
     - 在 "Environment Variables" 中添加 `.env` 文件中的所有变量
   - 点击 "Deploy"

4. **分享链接**
   - 部署完成后，Vercel 会提供一个网址，例如：`https://your-project.vercel.app`
   - 将这个链接分享给您的伴侣即可

### 优点：
- ✅ 完全免费
- ✅ 自动 HTTPS
- ✅ 全球 CDN 加速
- ✅ 自动部署（Git 推送后自动更新）

---

## 方式二：部署到 Netlify

Netlify 也是很好的免费托管平台。

### 步骤：

1. **准备代码**（同上）

2. **注册 Netlify**
   - 访问 https://www.netlify.com/
   - 使用 GitHub/GitLab 账号登录

3. **部署项目**
   - 点击 "Add new site" → "Import an existing project"
   - 选择您的 Git 仓库
   - 配置构建设置：
     - Build command: `npm run build`
     - Publish directory: `dist`
   - 添加环境变量（如果使用 Firebase）
   - 点击 "Deploy site"

4. **分享链接**
   - 部署完成后会得到一个网址，例如：`https://your-project.netlify.app`
   - 分享给伴侣即可

---

## 方式三：使用 GitHub Pages

如果您的代码在 GitHub 上，可以使用 GitHub Pages。

### 步骤：

1. **安装 gh-pages 包**
   ```bash
   npm install --save-dev gh-pages
   ```

2. **修改 package.json**
   添加以下脚本：
   ```json
   {
     "scripts": {
       "predeploy": "npm run build",
       "deploy": "gh-pages -d dist"
     },
     "homepage": "https://your-username.github.io/trip-app"
   }
   ```

3. **部署**
   ```bash
   npm run deploy
   ```

4. **启用 GitHub Pages**
   - 在 GitHub 仓库设置中，找到 "Pages"
   - Source 选择 "gh-pages" 分支
   - 保存后，访问 `https://your-username.github.io/trip-app`

---

## 方式四：打包成桌面应用（Electron）

如果想做成桌面应用，可以使用 Electron。

### 步骤：

1. **安装 Electron**
   ```bash
   npm install --save-dev electron electron-builder
   ```

2. **创建 Electron 主进程文件**（需要额外配置）

3. **打包**
   ```bash
   npm run build:electron
   ```

4. **分享安装包**
   - 打包后会生成 `.exe`（Windows）或 `.dmg`（Mac）文件
   - 发送给伴侣安装即可

---

## 方式五：本地运行（开发模式）

如果只是临时测试，可以让伴侣在本地运行。

### 步骤：

1. **分享代码**
   - 将整个 `trip-app` 文件夹压缩发送
   - 或通过 Git 仓库分享

2. **伴侣的操作**
   ```bash
   # 1. 解压文件
   # 2. 打开终端，进入项目目录
   cd trip-app
   
   # 3. 安装依赖
   npm install
   
   # 4. 配置 Firebase（如果使用）
   # 创建 .env 文件并添加配置
   
   # 5. 启动开发服务器
   npm run dev
   ```

3. **访问**
   - 打开浏览器访问 `http://localhost:5173`

---

## 推荐方案对比

| 方式 | 难度 | 优点 | 缺点 |
|------|------|------|------|
| **Vercel** | ⭐ 简单 | 免费、自动部署、全球加速 | 需要 Git 仓库 |
| **Netlify** | ⭐ 简单 | 免费、功能强大 | 需要 Git 仓库 |
| **GitHub Pages** | ⭐⭐ 中等 | 免费、与 Git 集成 | 需要 GitHub 账号 |
| **Electron** | ⭐⭐⭐ 复杂 | 独立应用 | 需要打包、文件较大 |
| **本地运行** | ⭐⭐ 中等 | 无需部署 | 需要安装 Node.js |

---

## 推荐流程（最简单）

1. **创建 GitHub 仓库**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   # 在 GitHub 创建新仓库，然后：
   git remote add origin https://github.com/your-username/trip-app.git
   git push -u origin main
   ```

2. **部署到 Vercel**
   - 访问 vercel.com
   - 导入 GitHub 仓库
   - 一键部署

3. **分享链接**
   - 将 Vercel 提供的网址发给伴侣
   - 她打开浏览器就能使用

---

## 注意事项

1. **环境变量**：如果使用 Firebase，记得在部署平台添加环境变量
2. **构建命令**：确保 `npm run build` 能正常运行
3. **路由问题**：如果使用 React Router，需要配置重定向规则（Vercel/Netlify 会自动处理）

---

## 快速开始（5分钟部署）

如果您想快速部署，推荐使用 Vercel：

1. 将代码推送到 GitHub
2. 访问 https://vercel.com/new
3. 导入仓库
4. 点击 Deploy
5. 完成！分享链接给伴侣

就是这么简单！🎉

